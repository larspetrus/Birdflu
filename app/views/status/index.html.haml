%h2 Various application stats/diagnostix

.stat Uptime: #{AlgSetLab.duration_to_s(Time.now - Birdflu::BOOTED_AT)}

.stat Ruby version: #{@ruby_version}

.stat Rails version: #{Rails.version}

.stat Cache #{number_to_human_size Rails.cache.instance_variable_get('@cache_size')}

.stat PID #{Process.pid}

.stat
  %a{href: "https://github.com/larspetrus/Birdflu/commit/#{`git rev-parse HEAD`}"}
    Git commit

.stat #{@raw_alg_count} algs, #{@combo_alg_count} combos, #{WcaUserData.count} users


:javascript
  $('body').addClass('status-page');